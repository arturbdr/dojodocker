apiVersion: v1
kind: Service
metadata:
  name: dojodocker-service
  namespace: dojodocker-namespace
spec:
  selector:
    app: dojodocker-app
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP
